# Vercel gateway

This directory gives the project a Vercel URL while keeping one canonical
database and ingestion queue. Every path and query is transparently served by
the production Transcript Registry.
